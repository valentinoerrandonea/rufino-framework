# Rufino v0.2.1 — Plan de fixes consolidado (Claude + Codex)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar los 3 críticos y los importantes de alto impacto reportados por los reviews de Claude y Codex sobre `feat/v0.2.0`, antes de promover a v0.2.1.

**Architecture:** Conservar la arquitectura actual (no hay defectos de diseño). Todos los fixes son cirugía local: 1-30 líneas por hotspot, sumados a un test de regresión que prueba el contrato que se rompió. La disciplina del TransactionLog se preserva en cada cambio que toca disco. Se mantiene "embeddings off by default" como ruta conservadora del wizard.

**Tech Stack:** Python 3.12+, pytest, Click, PyYAML, dataclasses(frozen), `fcntl.flock`, `subprocess.run`, `tempfile`, `xml.sax.saxutils`, sentence-transformers (opcional). TDD obligatorio: red → green → refactor → commit.

**Fuentes:**

- `docs/codereview/claudecode.md` — 3 críticos + 22 importantes + 22 minores
- `docs/codereview/codex.md` — 2 P0 + 4 P1 + 2 P2

**Mapeo cruzado de hallazgos** (sólo los que entran en este plan):

| Plan ID | Claude ID | Codex ID | Severidad | Archivo principal |
| --- | --- | --- | --- | --- |
| F1 | C1 | P0 #1 | CRIT | `mcp_server/tools.py`, `engine/query/api.py` |
| F2 | C2 | P1 (cron prefix) | CRIT | `runtime/scheduler/cron.py` |
| F3 | C3 | P0 #2 | CRIT | `wizard/spec_schema.py`, `wizard/adapter_materializers/ingest.py`, `engine/ingest/manifest.py` |
| F4 | I-A | — | HIGH | `engine/process/batch/dispatcher.py`, `runner_helper.py`, `consolidator.py`, `retry.py`, `qa_resume.py` |
| F5 | I-G + I-H | P1 #4 | HIGH | `runtime/embedder/cross_encoder.py`, `engine/query/api.py` |
| F6 | — | P1 #3 | HIGH | `cli.py` (`enable-embeddings`), `wizard/operative_rules.md`, `wizard/system_prompt_assembler.py` |
| F7 | I-B | — | HIGH | `wizard/spec_schema.py`, `wizard/adapter_materializers/ingest.py` |
| F8 | I-C | — | HIGH | `wizard/spec_schema.py`, `wizard/adapter_materializers/{ingest,process}.py` |
| F9 | I-D + scheduler "paths" | P2 #1 | HIGH | `cli.py` (`install_ingest_cmd`) |
| F10 | I-E | — | MED | `runtime/scheduler/launchd.py` |
| F11 | I-F | — | MED | `cli.py` (`enable_embeddings_cmd`, `disable_embeddings_cmd`) |
| F12 | (Phase 2 important #4) | P2 #2 | MED | `cli.py` (`materialize_cmd`), `engine/query/semantic.py` |
| F13 | doc drift | — | LOW | `CLAUDE.md`, `docs/runtime.md`, `docs/primitives/query.md`, `wizard/post_bootstrap_docs.py` |

**Reglas de versionado:** Bumpear `src/rufino/version.py` y `pyproject.toml` a `0.2.1` en el último commit del bloque. Migración: no es necesaria — todos los fixes son backwards-compatible salvo F3 (manifest shape), que se resuelve aceptando ambas formas en el parser durante la transición.

---

## Tarea F1: `find_note` MCP — fallback a lexical cuando no hay embeddings

**Files:**

- Modify: `src/rufino/mcp_server/tools.py:12-14`
- Modify: `src/rufino/engine/query/api.py:43-47` (opcional según opción elegida)
- Test: `tests/test_mcp_tools.py` (nuevo test)

**Hallazgo:** `find_note(query)` codea `mode="hybrid"` y el schema MCP no expone el parámetro. En un vault recién materializado (embeddings off), `QueryLayer.search` ruta a `NotImplementedError` y el server lo re-lanza como `ValueError`. Es el primer tool call de Claude Code contra un vault virgen → falla siempre.

- [ ] **Step 1: Escribir test que falle**

Agregar al archivo `tests/test_mcp_tools.py`:

```python
def test_find_note_works_on_vault_without_embeddings(tmp_path: Path) -> None:
    """Regression: C1 from codereview — find_note debe funcionar en vault virgen."""
    from rufino.engine.query.api import QueryLayer
    from rufino.runtime.embedder.resolve import NoopEmbedder
    from rufino.mcp_server.tools import find_note

    vault = tmp_path / "vault"
    vault.mkdir()
    (vault / "alpha.md").write_text("# alpha\nHola desde alpha.", encoding="utf-8")
    (vault / "beta.md").write_text("# beta\nNada que ver.", encoding="utf-8")

    ql = QueryLayer(vault_root=vault, embedder=NoopEmbedder())
    # No rebuild — vault virgen. find_note no debe lanzar.
    result = find_note(ql, query="alpha")
    assert result == "alpha.md"
```

- [ ] **Step 2: Correr test para verificar fallo**

```bash
pytest tests/test_mcp_tools.py::test_find_note_works_on_vault_without_embeddings -v
```

Esperado: FAIL con `NotImplementedError` o `ValueError` propagado desde `QueryLayer.search`.

- [ ] **Step 3: Aplicar fix mínimo**

Editar `src/rufino/mcp_server/tools.py:12-14`:

```python
def find_note(ql: QueryLayer, *, query: str) -> str | None:
    mode = "hybrid" if ql.embeddings_enabled() else "lexical"
    results = ql.search(query, mode=mode, k=1)
    return results[0].relative_path if results else None
```

Y agregar el helper en `src/rufino/engine/query/api.py` (justo antes de `def search`):

```python
def embeddings_enabled(self) -> bool:
    """True si el embedder configurado puede producir vectores reales."""
    from rufino.runtime.embedder.resolve import NoopEmbedder
    return not isinstance(self._embedder, NoopEmbedder)
```

Si el atributo se llama distinto (`self._sem.embedder` o similar), ajustar el path. Verificar antes con `rg 'self\._embedder|self\._sem' src/rufino/engine/query/api.py`.

- [ ] **Step 4: Aplicar el mismo guard a `search_vault`**

`search_vault` también acepta `mode="hybrid"` como default — aplicar el mismo fallback para tools call vía MCP cuando el cliente no especifica modo. Modificar `src/rufino/mcp_server/tools.py:7-9`:

```python
def search_vault(ql: QueryLayer, *, query: str, mode: str = "auto", k: int = 10) -> list[str]:
    if mode == "auto":
        mode = "hybrid" if ql.embeddings_enabled() else "lexical"
    results = ql.search(query, mode=mode, k=k)
    return [r.relative_path for r in results]
```

- [ ] **Step 5: Correr tests, deben pasar**

```bash
pytest tests/test_mcp_tools.py -v
```

- [ ] **Step 6: Commit**

```bash
git add src/rufino/mcp_server/tools.py src/rufino/engine/query/api.py tests/test_mcp_tools.py
git commit -m "fix(mcp): find_note/search_vault hacen fallback a lexical en vault sin embeddings"
```

---

## Tarea F2: cron uninstall — comparar marker exacto

**Files:**

- Modify: `src/rufino/runtime/scheduler/cron.py:88-93` y `cron.py:53-61`
- Test: `tests/test_scheduler_cron.py` (extender)

**Hallazgo:** `_filter_other_entries` usa `marker not in line` (substring), lo que borra `# rufino-job:foobar` cuando se desinstala `foo`. `list_jobs` tiene la misma clase de bug con `line.find`.

- [ ] **Step 1: Escribir test que falle**

Agregar a `tests/test_scheduler_cron.py`:

```python
def test_uninstall_does_not_remove_prefix_sibling() -> None:
    """Regression: C2 — uninstall job_id='foo' no debe borrar 'foobar'."""
    backend = CronBackend(runner=_recording_runner_with_initial(""))
    backend.install(job_id="foo", schedule="* * * * *", cmd="echo a", log_path="/tmp/a")
    backend.install(job_id="foobar", schedule="* * * * *", cmd="echo b", log_path="/tmp/b")
    assert sorted(backend.list_jobs()) == ["foo", "foobar"]

    backend.uninstall(job_id="foo")
    assert backend.list_jobs() == ["foobar"]


def test_list_jobs_ignores_marker_inside_cmd_body() -> None:
    """Regression: list_jobs solo debe extraer el marker al final de la línea."""
    fake_crontab = (
        "* * * * * echo '# rufino-job:fake' >> /tmp/x 2>&1 # rufino-job:real\n"
    )
    backend = CronBackend(runner=_recording_runner_with_initial(fake_crontab))
    assert backend.list_jobs() == ["real"]
```

Si `_recording_runner_with_initial` no existe ya en el archivo, escribir un helper mínimo arriba:

```python
def _recording_runner_with_initial(initial: str):
    state = {"content": initial}
    def runner(cmd, *, input=None):
        if cmd[:2] == ["crontab", "-l"]:
            if not state["content"]:
                return subprocess.CompletedProcess(cmd, returncode=1, stdout="", stderr="")
            return subprocess.CompletedProcess(cmd, returncode=0, stdout=state["content"], stderr="")
        if cmd[:2] == ["crontab", "-"]:
            state["content"] = input or ""
            return subprocess.CompletedProcess(cmd, returncode=0, stdout="", stderr="")
        return subprocess.CompletedProcess(cmd, returncode=2, stdout="", stderr="")
    return runner
```

- [ ] **Step 2: Correr test para verificar fallo**

```bash
pytest tests/test_scheduler_cron.py::test_uninstall_does_not_remove_prefix_sibling tests/test_scheduler_cron.py::test_list_jobs_ignores_marker_inside_cmd_body -v
```

Esperado: FAIL. El primero borra ambos; el segundo reporta `fake` en vez de `real`.

- [ ] **Step 3: Aplicar fix exacto**

Reemplazar `_filter_other_entries` en `src/rufino/runtime/scheduler/cron.py:88-97`:

```python
def _filter_other_entries(content: str, *, job_id: str) -> str:
    target = f"{_MARKER_PREFIX}{job_id}"
    kept: list[str] = []
    for line in content.splitlines(keepends=True):
        body = line.rstrip("\n").rstrip("\r")
        idx = body.rfind(_MARKER_PREFIX)
        line_job = body[idx:].strip() if idx != -1 else ""
        if line_job == target:
            continue
        kept.append(line)
    out = "".join(kept)
    if out and not out.endswith("\n"):
        out += "\n"
    return out
```

Reemplazar el loop de `list_jobs` en `cron.py:53-61` para usar la misma lógica del marker al final:

```python
def list_jobs(self) -> list[str]:
    current = self._read_crontab()
    jobs: list[str] = []
    for line in current.splitlines():
        body = line.rstrip()
        idx = body.rfind(_MARKER_PREFIX)
        if idx == -1:
            continue
        jobs.append(body[idx + len(_MARKER_PREFIX):].strip())
    return sorted(jobs)
```

- [ ] **Step 4: Correr tests, todos pasan**

```bash
pytest tests/test_scheduler_cron.py -v
```

- [ ] **Step 5: Commit**

```bash
git add src/rufino/runtime/scheduler/cron.py tests/test_scheduler_cron.py
git commit -m "fix(scheduler): cron compara marker exacto al final de línea (evita colisión de prefijo)"
```

---

## Tarea F3: wizard `emit_facts` — alinear contrato manifest

**Files:**

- Modify: `src/rufino/wizard/spec_schema.py:182-196`
- Modify: `src/rufino/wizard/adapter_materializers/ingest.py:82-89`
- Modify: `src/rufino/engine/ingest/manifest.py:75` (aceptar `dedup_by` como lista o str)
- Test: `tests/test_wizard_materialize_ingest.py` (nuevo)
- Test: `tests/test_wizard_spec_schema.py` (extender)

**Hallazgo:** `spec_schema` valida `destination` como `str` y `dedup_by` como tuple; `parse_ingest_manifest` espera `destination` como mapping `{facts, raw}` y `dedup_by` como str. Toda spec wizard con `emit_facts` falla en runtime con `AttributeError`.

**Decisión:** El contrato del runtime (mapping + str) es más expresivo (separa `facts.jsonl` de `raw.jsonl`). Mover la wizard a esa shape. Tolerar la shape vieja en el parser durante el ciclo.

- [ ] **Step 1: Escribir test end-to-end que falle**

Crear `tests/test_wizard_materialize_ingest.py`:

```python
from pathlib import Path

from rufino.engine.ingest.manifest import parse_ingest_manifest
from rufino.runtime.transaction_log import TransactionLog
from rufino.wizard.adapter_materializers.ingest import materialize_ingest
from rufino.wizard.spec_schema import validate_spec


def _emit_facts_spec_dict() -> dict:
    return {
        "vertical_name": "demo",
        "patterns": ["discrete_events_with_metadata"],
        "entities": ["person"],
        "sources": [{
            "adapter_name": "drive_meet",
            "source_name": "google-drive",
            "output_mode": "emit_facts",
            "schedule": "*/15 * * * *",
            "auth": {"keychain_label": "x"},
            "emits": ["meeting"],
            "fact_schema": {"meeting": {"id": "str"}},
            "destination": {"facts": "_data/meetings.jsonl", "raw": "_data/raw.jsonl"},
            "dedup_by": "id",
        }],
        "processing": [],
        "outputs": [],
        "vocabulary": {"person": "personas/{name}.md"},
    }


def test_emit_facts_spec_round_trips_through_materializer(tmp_path: Path) -> None:
    """Regression: C3 — wizard emit_facts genera un manifest que el engine acepta."""
    spec = validate_spec(_emit_facts_spec_dict())
    tx = TransactionLog(tmp_path / "tx.json")
    adapter_dir = materialize_ingest(
        spec=spec.sources[0], base_dir=tmp_path / "rufino_home",
        vault_slug="demo", tx_log=tx,
    )
    yaml_text = (adapter_dir / "manifest.yaml").read_text(encoding="utf-8")
    manifest = parse_ingest_manifest(yaml_text)
    assert manifest.destination_facts == "_data/meetings.jsonl"
    assert manifest.destination_raw == "_data/raw.jsonl"
    assert manifest.dedup_by == "id"
```

Extender `tests/test_wizard_spec_schema.py` con:

```python
def test_emit_facts_requires_destination_mapping_and_string_dedup() -> None:
    spec = {
        "vertical_name": "v", "patterns": ["discrete_events_with_metadata"],
        "entities": [], "sources": [{
            "adapter_name": "a", "source_name": "x", "output_mode": "emit_facts",
            "auth": {}, "emits": ["meeting"], "fact_schema": {"meeting": {}},
            "destination": "facts.jsonl",  # string — debe ser rechazada
            "dedup_by": "id",
        }], "processing": [], "outputs": [], "vocabulary": {},
    }
    with pytest.raises(SpecError, match="destination"):
        validate_spec(spec)
```

- [ ] **Step 2: Correr tests para verificar fallo**

```bash
pytest tests/test_wizard_materialize_ingest.py tests/test_wizard_spec_schema.py::test_emit_facts_requires_destination_mapping_and_string_dedup -v
```

Esperado: FAIL (el primero por `AttributeError`/`ManifestParseError`; el segundo por `validate_spec` aceptando la string).

- [ ] **Step 3: Cambiar `spec_schema.py` para exigir mapping + str**

En `src/rufino/wizard/spec_schema.py:192-196`, reemplazar:

```python
        kwargs["destination"] = _require_str(entry, "destination", where=where)
        dedup_by = entry.get("dedup_by", [])
        if not isinstance(dedup_by, list):
            raise SpecError(f"{where}: dedup_by must be a list")
        kwargs["dedup_by"] = tuple(dedup_by)
```

por:

```python
        destination = _require_mapping(entry, "destination", where=where)
        if "facts" not in destination or not isinstance(destination["facts"], str):
            raise SpecError(f"{where}: destination.facts must be a string path")
        if "raw" in destination and not isinstance(destination["raw"], str):
            raise SpecError(f"{where}: destination.raw must be a string path if present")
        kwargs["destination"] = _freeze_deep(destination)
        kwargs["dedup_by"] = _require_str(entry, "dedup_by", where=where)
```

En la dataclass `IngestSpec` (líneas 28-48), cambiar:

```python
    destination: str | None = None
    dedup_by: tuple[str, ...] | None = None
```

por:

```python
    destination: Mapping[str, Any] | None = None
    dedup_by: str | None = None
```

- [ ] **Step 4: Actualizar el materializer**

En `src/rufino/wizard/adapter_materializers/ingest.py:86-89`, reemplazar:

```python
    if spec.destination is not None:
        d["destination"] = spec.destination
    if spec.dedup_by is not None:
        d["dedup_by"] = list(spec.dedup_by)
```

por:

```python
    if spec.destination is not None:
        d["destination"] = _to_plain(spec.destination)
    if spec.dedup_by is not None:
        d["dedup_by"] = spec.dedup_by
```

- [ ] **Step 5: Tolerar la shape vieja en el parser (defensa)**

En `src/rufino/engine/ingest/manifest.py:64-76`, hacer que `destination` string siga funcionando con un warning (o mantener el contrato estricto si preferimos romper duro):

```python
    if mode == "emit_fact":
        for f in ("emits", "fact_schema", "destination", "dedup_by"):
            if f not in raw:
                raise ManifestParseError(f"emit_fact requires field {f!r}")
        dest = raw["destination"]
        if isinstance(dest, str):
            destination_facts, destination_raw = dest, None
        elif isinstance(dest, dict):
            destination_facts = dest.get("facts")
            destination_raw = dest.get("raw")
            if not isinstance(destination_facts, str):
                raise ManifestParseError("destination.facts must be a string path")
        else:
            raise ManifestParseError(
                f"destination must be a string or mapping with 'facts'/'raw', got {type(dest).__name__}"
            )
        dedup_by = raw["dedup_by"]
        if not isinstance(dedup_by, str):
            raise ManifestParseError("dedup_by must be a string field name")
        return IngestAdapterManifest(
            **common,
            emits=tuple(raw["emits"]),
            fact_schema=raw["fact_schema"],
            destination_facts=destination_facts,
            destination_raw=destination_raw,
            dedup_by=dedup_by,
        )
```

- [ ] **Step 6: Actualizar el system prompt para que las specs generadas coincidan**

`src/rufino/wizard/system_prompt_assembler.py` describe la shape de `destination` y `dedup_by` (revisar líneas alrededor de `emit_facts`). Asegurar que el ejemplo / contrato muestra `destination: {facts: ..., raw: ...}` y `dedup_by: "id"`. Buscar primero:

```bash
rg -n 'destination|dedup_by|emit_facts' src/rufino/wizard/system_prompt_assembler.py src/rufino/wizard/operative_rules.md
```

y corregir cualquier ejemplo / descripción.

- [ ] **Step 7: Correr tests, deben pasar**

```bash
pytest tests/test_wizard_materialize_ingest.py tests/test_wizard_spec_schema.py tests/test_ingest_manifest.py -v
```

- [ ] **Step 8: Commit**

```bash
git add src/rufino/wizard/spec_schema.py src/rufino/wizard/adapter_materializers/ingest.py src/rufino/engine/ingest/manifest.py src/rufino/wizard/system_prompt_assembler.py src/rufino/wizard/operative_rules.md tests/test_wizard_materialize_ingest.py tests/test_wizard_spec_schema.py
git commit -m "fix(wizard): emit_facts → destination mapping + dedup_by string (alinear con engine)"
```

---

## Tarea F4: Wirear `run_claude_worker` para bounded I/O real

**Files:**

- Modify: `src/rufino/engine/process/batch/dispatcher.py:77`
- Modify: `src/rufino/engine/process/batch/consolidator.py:105`
- Modify: `src/rufino/engine/process/batch/retry.py:84`
- Modify: `src/rufino/engine/process/batch/qa_resume.py:229`
- Modify: `src/rufino/engine/process/batch/runner_helper.py` (signature compatible)
- Test: `tests/test_runner_helper_bounded_io.py` (nuevo)
- Test: `tests/test_dispatcher_uses_bounded_io.py` (nuevo)

**Hallazgo:** `run_claude_worker` (streaming + cap 1 MB) existe y está testeado pero ningún caller productivo lo invoca. `run_claude` buferea el stdout completo y recién trunca al final — un worker runaway puede OOM el padre. CLAUDE.md anuncia "Bounded I/O — worker stdout/stderr capped at MAX_OUTPUT_BYTES (1MB)" como deliverable v0.2.0.

- [ ] **Step 1: Escribir test que falle**

Crear `tests/test_dispatcher_uses_bounded_io.py`:

```python
import inspect
import textwrap

import rufino.engine.process.batch.dispatcher as dispatcher_mod
import rufino.engine.process.batch.consolidator as consolidator_mod
import rufino.engine.process.batch.retry as retry_mod
import rufino.engine.process.batch.qa_resume as qa_mod


def _source(mod):
    return textwrap.dedent(inspect.getsource(mod))


def test_no_production_caller_uses_unbounded_run_claude() -> None:
    """Regression: I-A — bounded I/O debe ser la ruta productiva."""
    for mod in (dispatcher_mod, consolidator_mod, retry_mod, qa_mod):
        src = _source(mod)
        assert "await run_claude(" not in src, (
            f"{mod.__name__} aún usa run_claude (unbounded). Usar run_claude_worker."
        )
```

(Test pragmático — assertion sobre el source. Más adelante reemplazable por tests de comportamiento sobre `MAX_OUTPUT_BYTES`).

- [ ] **Step 2: Correr test para verificar fallo**

```bash
pytest tests/test_dispatcher_uses_bounded_io.py -v
```

Esperado: FAIL — los cuatro módulos aún usan `run_claude`.

- [ ] **Step 3: Decidir estrategia de migración**

Hacer que `run_claude` delegue internamente en `run_claude_worker` y devuelva la misma `ClaudeResult` que esperan los callers. Editar `src/rufino/engine/process/batch/runner_helper.py`:

```python
async def run_claude(*args, **kwargs) -> ClaudeResult:
    """Backwards-compatible wrapper: delegates to run_claude_worker (bounded)."""
    worker_result = await run_claude_worker(*args, **kwargs)
    if worker_result.truncated:
        # surfaceable warning — el caller puede inspeccionar `truncated` en logs
        import logging
        logging.getLogger(__name__).warning(
            "claude worker output truncated at %d bytes", MAX_OUTPUT_BYTES
        )
    return ClaudeResult(
        stdout=worker_result.stdout,
        stderr=worker_result.stderr,
        returncode=worker_result.returncode,
        truncated=worker_result.truncated,
    )
```

Si `ClaudeResult` no tiene `truncated` todavía, agregarlo:

```python
@dataclass(frozen=True)
class ClaudeResult:
    stdout: str
    stderr: str
    returncode: int
    truncated: bool = False
```

- [ ] **Step 4: Migrar cada caller a la API nueva (sin cambio de comportamiento)**

Cada caller mantiene `await run_claude(...)`. El cambio es transparente porque la signature se preservó. Si la firma cambió (kwargs distintos entre `run_claude` y `run_claude_worker`), reconciliar caller-por-caller. Verificar antes:

```bash
rg -n 'await run_claude\(' src/rufino/engine/process/batch/
```

Y leer los 4 sitios para asegurar que los args coinciden.

Si preferimos el camino más explícito (recomendado para evitar el "wrapper que tapa"): reemplazar el import y la llamada en cada uno de los 4 callers, sustituyendo `run_claude` por `run_claude_worker`. El test de Step 1 ya cubre esa migración.

- [ ] **Step 5: Surfaceá `truncated` en el resumen del run**

En `src/rufino/engine/process/batch/dispatcher.py` (cerca del logging del summary del batch), agregar:

```python
truncated_count = sum(1 for r in results if r.truncated)
if truncated_count:
    click.echo(
        f"warning: {truncated_count} worker(s) tuvieron output truncado "
        f"a {MAX_OUTPUT_BYTES} bytes — revisar logs si el output incompleto "
        f"causó fallos downstream."
    )
```

(Localizar el sitio exacto del summary con `rg -n 'workers completed|batch.*done|summary' src/rufino/engine/process/batch/dispatcher.py`).

- [ ] **Step 6: Correr tests**

```bash
pytest tests/test_dispatcher_uses_bounded_io.py tests/test_runner_helper_bounded_io.py -v
pytest tests/ -k "process_batch or dispatcher or runner_helper" -v
```

- [ ] **Step 7: Commit**

```bash
git add src/rufino/engine/process/batch/ tests/test_dispatcher_uses_bounded_io.py
git commit -m "fix(process): wirear run_claude_worker — bounded I/O real en producción"
```

---

## Tarea F5: Hardening del cross-encoder (pin + errores + lazy load)

**Files:**

- Modify: `src/rufino/runtime/embedder/cross_encoder.py:12-18`
- Modify: `src/rufino/engine/query/api.py:48-71`
- Test: `tests/test_cross_encoder.py` (extender)

**Hallazgo:** Tres problemas combinados:

- **I-G**: `CrossEncoder("BAAI/bge-reranker-base")` sin `revision=` → primer query híbrido descarga ~400 MB silenciosamente.
- **I-H** + **Codex P1 #4**: `engine/query/api.py:71` solo captura `ImportError`. `OSError` / `RuntimeError` / fallos de Hugging Face (red, disco) → crash opaco. En tests no mockeados, la importación de `torch` vía `sentence_transformers` abortó el proceso de pytest entero en el entorno del reviewer.
- Reinstantiación por query: el cache lazy se tira en cada call.

- [ ] **Step 1: Escribir test que falle**

Agregar a `tests/test_cross_encoder.py`:

```python
import builtins

import pytest
import rufino.engine.query.api as api_mod


def test_hybrid_falls_back_when_cross_encoder_fails_with_oserror(monkeypatch, tmp_path) -> None:
    """Regression: I-H — fallos no-ImportError del reranker no deben crashear."""
    from rufino.engine.query.api import QueryLayer
    from rufino.runtime.embedder.resolve import NoopEmbedder

    # Simular que CrossEncoderReranker.rerank lanza OSError
    def boom(self, query, candidates):  # noqa: ARG001
        raise OSError("hub network down")
    monkeypatch.setattr(api_mod.CrossEncoderReranker, "rerank", boom)

    vault = tmp_path / "v"
    vault.mkdir()
    (vault / "a.md").write_text("alpha note")
    ql = QueryLayer(vault_root=vault, embedder=NoopEmbedder())
    results = ql.search("alpha", mode="hybrid", k=5)
    assert len(results) >= 1  # fallback a union order, no crash


def test_cross_encoder_pins_revision() -> None:
    """Regression: I-G — el modelo cross-encoder debe estar pinned por revisión."""
    import rufino.runtime.embedder.cross_encoder as ce
    import inspect
    src = inspect.getsource(ce)
    assert 'revision=' in src, "CrossEncoder debe pasar revision='<sha>' por reproducibilidad"
```

- [ ] **Step 2: Correr tests para verificar fallo**

```bash
pytest tests/test_cross_encoder.py -v -k "fallback_when_cross_encoder_fails or pins_revision"
```

- [ ] **Step 3: Pinear el modelo y aceptar override por env**

Editar `src/rufino/runtime/embedder/cross_encoder.py`:

```python
import os

_DEFAULT_MODEL = "BAAI/bge-reranker-base"
# Pin commit SHA de Hugging Face — actualizar con el último estable revisado.
_DEFAULT_REVISION = "ce0834b22b89e8b58ef0a2cce6d2f7a76e1a3e76"  # bge-reranker-base @ 2024-12

class CrossEncoderReranker:
    def _load_model(self):
        if self._model is None:
            from sentence_transformers import CrossEncoder
            model_name = os.environ.get("RUFINO_RERANKER_MODEL", _DEFAULT_MODEL)
            revision = os.environ.get("RUFINO_RERANKER_REVISION", _DEFAULT_REVISION)
            self._model = CrossEncoder(model_name, revision=revision)
        return self._model
```

Validar el commit SHA actual del modelo con `huggingface-cli` o desde la página del modelo antes de pinearlo. Documentar el SHA elegido en `docs/cli-reference.md` (sección "embeddings opcionales").

- [ ] **Step 4: Ensanchar el catch en `query/api.py`**

En `src/rufino/engine/query/api.py:60-71`, reemplazar:

```python
    try:
        reranker = CrossEncoderReranker()
        reranked = reranker.rerank(query, by_content_list)
    except ImportError:
        # sentence_transformers no instalado → fallback orden de unión
        ...
```

por:

```python
    try:
        reranker = self._get_reranker()
        reranked = reranker.rerank(query, by_content_list)
    except (ImportError, OSError, RuntimeError) as e:
        import logging
        logging.getLogger(__name__).warning(
            "cross-encoder rerank no disponible (%s) — usando orden de unión", e
        )
        reranked = by_content_list  # mantener orden original
```

Y agregar el cache en `QueryLayer.__init__` o como property:

```python
def _get_reranker(self):
    if self._reranker is None:
        from rufino.runtime.embedder.cross_encoder import CrossEncoderReranker
        self._reranker = CrossEncoderReranker()
    return self._reranker
```

(con `self._reranker: CrossEncoderReranker | None = None` en `__init__`).

- [ ] **Step 5: Correr tests, todos pasan**

```bash
pytest tests/test_cross_encoder.py tests/test_query_api.py -v
```

- [ ] **Step 6: Commit**

```bash
git add src/rufino/runtime/embedder/cross_encoder.py src/rufino/engine/query/api.py tests/test_cross_encoder.py docs/cli-reference.md
git commit -m "fix(embedder): pin cross-encoder revision + ensanchar catch (ImportError/OSError/RuntimeError) + cache reranker"
```

---

## Tarea F6: `enable-embeddings` — `--state-dir` con default

**Files:**

- Modify: `src/rufino/cli.py:333-340` (`enable_embeddings_cmd`)
- Modify: `src/rufino/cli.py` (`disable_embeddings_cmd` — verificar igual problema)
- Test: `tests/test_cli_enable_embeddings.py` (extender)

**Hallazgo (Codex P1 #3):** El wizard instruye `rufino enable-embeddings --vault <vault>` pero la CLI requiere `--state-dir`. El comando del flujo guiado falla. `query` / `mcp-server` / `output` ya tienen default; alinear.

- [ ] **Step 1: Escribir test que falle**

Agregar a `tests/test_cli_enable_embeddings.py`:

```python
def test_enable_embeddings_works_without_state_dir_flag(tmp_path, monkeypatch) -> None:
    """Regression: Codex P1 #3 — wizard usa enable-embeddings sin --state-dir."""
    monkeypatch.setenv("HOME", str(tmp_path))  # forzar ~/.rufino dentro de tmp
    monkeypatch.setattr("rufino.runtime.embedder.detect.detect_ollama",
                        lambda model: type("D", (), {"ok": True, "reason": None})())
    monkeypatch.setattr("rufino.engine.query.api.QueryLayer.rebuild_indices",
                        lambda self: None)
    vault = tmp_path / "vault"; vault.mkdir()

    from click.testing import CliRunner
    from rufino.cli import enable_embeddings_cmd
    runner = CliRunner()
    result = runner.invoke(enable_embeddings_cmd, ["--vault", str(vault)])
    assert result.exit_code == 0, result.output
```

- [ ] **Step 2: Correr test para verificar fallo**

```bash
pytest tests/test_cli_enable_embeddings.py::test_enable_embeddings_works_without_state_dir_flag -v
```

Esperado: FAIL — Click rechaza por `--state-dir` faltante.

- [ ] **Step 3: Aplicar fix**

Editar `src/rufino/cli.py:333-340` (y `disable_embeddings_cmd` idéntico):

```python
@cli.command("enable-embeddings")
@click.option("--vault", required=True, type=click.Path(path_type=Path))
@click.option(
    "--state-dir",
    default=lambda: Path.home() / ".rufino" / "state",
    type=click.Path(path_type=Path),
    show_default="~/.rufino/state",
)
def enable_embeddings_cmd(vault: Path, state_dir: Path) -> None:
    ...
```

Verificar que `query`, `mcp-server`, `output` ya tienen ese mismo patrón (default = `~/.rufino/state`). Si difieren, alinear todos.

- [ ] **Step 4: Correr tests**

```bash
pytest tests/test_cli_enable_embeddings.py -v
```

- [ ] **Step 5: Commit**

```bash
git add src/rufino/cli.py tests/test_cli_enable_embeddings.py
git commit -m "fix(cli): enable/disable-embeddings reciben --state-dir con default (alinear con query/mcp-server)"
```

---

## Tarea F7: Wizard materializa `fetcher.py` (Ingest funcional out-of-the-box)

**Files:**

- Modify: `src/rufino/wizard/spec_schema.py` (agregar `fetcher_body: str | None`)
- Modify: `src/rufino/wizard/adapter_materializers/ingest.py` (escribir `fetcher.py`)
- Modify: `src/rufino/wizard/system_prompt_assembler.py` (instruir al wizard a producir el body)
- Test: `tests/test_wizard_materialize_ingest.py` (extender)

**Hallazgo (Claude I-B):** El materializer sólo escribe `manifest.yaml`. El runner de Ingest necesita un `fetcher.py` importable. Adapter wizard-materialized → `ModuleNotFoundError` en primer `rufino ingest`.

**Decisión:** Capturar `fetcher_body` opcional. Si está, escribirlo. Si no, materializar un scaffold con marker `# TODO: implementar fetch()` y fail-fast bien explicado en runtime.

- [ ] **Step 1: Escribir test que falle**

Extender `tests/test_wizard_materialize_ingest.py`:

```python
def test_materialize_ingest_writes_fetcher_when_body_provided(tmp_path: Path) -> None:
    spec_dict = _emit_facts_spec_dict()
    spec_dict["sources"][0]["fetcher_body"] = "def fetch(cursor):\n    return [], cursor\n"
    spec = validate_spec(spec_dict)
    tx = TransactionLog(tmp_path / "tx.json")
    adapter_dir = materialize_ingest(
        spec=spec.sources[0], base_dir=tmp_path / "h",
        vault_slug="demo", tx_log=tx,
    )
    fetcher = adapter_dir / "fetcher.py"
    assert fetcher.exists()
    assert "def fetch" in fetcher.read_text(encoding="utf-8")


def test_materialize_ingest_writes_scaffold_when_no_body(tmp_path: Path) -> None:
    spec = validate_spec(_emit_facts_spec_dict())
    tx = TransactionLog(tmp_path / "tx.json")
    adapter_dir = materialize_ingest(
        spec=spec.sources[0], base_dir=tmp_path / "h",
        vault_slug="demo", tx_log=tx,
    )
    fetcher = adapter_dir / "fetcher.py"
    assert fetcher.exists()
    assert "TODO" in fetcher.read_text(encoding="utf-8")
```

- [ ] **Step 2: Correr tests para verificar fallo**

```bash
pytest tests/test_wizard_materialize_ingest.py -v -k "fetcher"
```

- [ ] **Step 3: Agregar `fetcher_body` a `IngestSpec`**

En `src/rufino/wizard/spec_schema.py`:

```python
# en IngestSpec:
    fetcher_body: str | None = None

# en _validate_ingest, después del bloque de validaciones por modo:
    fetcher_body = entry.get("fetcher_body")
    if fetcher_body is not None and not isinstance(fetcher_body, str):
        raise SpecError(f"{where}: fetcher_body must be a string if provided")
    kwargs["fetcher_body"] = fetcher_body
```

- [ ] **Step 4: Escribir el archivo en el materializer**

En `src/rufino/wizard/adapter_materializers/ingest.py`, después del bloque que escribe `manifest.yaml`:

```python
fetcher_path = adapter_dir / "fetcher.py"
body = spec.fetcher_body or _SCAFFOLD_FETCHER
apply_and_log(
    tx_log,
    op="write",
    target=str(fetcher_path),
    apply_fn=lambda: fetcher_path.write_text(body, encoding="utf-8"),
    rollback="delete",
)
```

Y agregar la constante arriba del archivo:

```python
_SCAFFOLD_FETCHER = '''"""TODO: implementar fetch(cursor) para este adapter.

Contrato: ver docs/primitives/ingest.md. Hasta que esto se escriba,
`rufino ingest <adapter_dir>` lanzará NotImplementedError de forma explícita.
"""


def fetch(cursor):
    raise NotImplementedError(
        "fetcher.py no implementado — el wizard generó un scaffold. "
        "Editar este archivo para devolver (records, new_cursor)."
    )
'''
```

- [ ] **Step 5: Documentar el campo en el system prompt**

Editar `src/rufino/wizard/system_prompt_assembler.py` para indicarle a Claude que, cuando esté disponible, capture el body de `fetcher.py` y lo emita como `fetcher_body` en la spec.

- [ ] **Step 6: Correr tests**

```bash
pytest tests/test_wizard_materialize_ingest.py tests/test_wizard_spec_schema.py -v
```

- [ ] **Step 7: Commit**

```bash
git add src/rufino/wizard/ tests/test_wizard_materialize_ingest.py
git commit -m "feat(wizard): materializar fetcher.py (body opcional + scaffold con NotImplementedError)"
```

---

## Tarea F8: `transform_hook` accesible desde el wizard

**Files:**

- Modify: `src/rufino/wizard/spec_schema.py` (campo `transform_hook_body` en IngestSpec y ProcessSpec)
- Modify: `src/rufino/wizard/adapter_materializers/ingest.py` y `process.py` (escribir `transform.py` y agregar `transform_hook: transform.py` al manifest)
- Modify: `src/rufino/wizard/system_prompt_assembler.py` (instruir captura)
- Test: `tests/test_wizard_materialize_ingest.py` y `tests/test_wizard_materialize_process.py` (extender)

**Hallazgo (Claude I-C):** El runtime de `transform_hook` está implementado y bien testeado, pero el wizard es ciego — no expone el campo. Sólo se accede hand-editando el manifest post-materialización.

- [ ] **Step 1: Tests que fallan**

```python
def test_materialize_ingest_writes_transform_when_body_provided(tmp_path: Path) -> None:
    spec_dict = _emit_facts_spec_dict()
    spec_dict["sources"][0]["transform_hook_body"] = (
        "import json, sys\n"
        "rec = json.loads(sys.stdin.read())\n"
        "rec['extra'] = 'tagged'\n"
        "print(json.dumps(rec))\n"
    )
    spec = validate_spec(spec_dict)
    tx = TransactionLog(tmp_path / "tx.json")
    adapter_dir = materialize_ingest(spec=spec.sources[0], base_dir=tmp_path / "h",
                                     vault_slug="demo", tx_log=tx)
    assert (adapter_dir / "transform.py").exists()
    manifest = yaml.safe_load((adapter_dir / "manifest.yaml").read_text())
    assert manifest["transform_hook"] == "transform.py"
```

Análogo para Process.

- [ ] **Step 2-5: Fix análogo a F7 — campo en spec, escritura en materializer, manifest field**

Verificar en `engine/process/.../manifest.py` y `engine/ingest/manifest.py` que `transform_hook` ya está en la dataclass del manifest (en `IngestAdapterManifest` ya está, línea 30).

- [ ] **Step 6: Commit**

```bash
git add src/rufino/wizard/ tests/
git commit -m "feat(wizard): exponer transform_hook (body opcional → transform.py + manifest field)"
```

---

## Tarea F9: `install-ingest` — TransactionLog + paths absolutos + validar adapter_name

**Files:**

- Modify: `src/rufino/cli.py:565-602` (`install_ingest_cmd`)
- Modify: `src/rufino/engine/ingest/manifest.py:14-22` (validar `adapter_name`)
- Test: `tests/test_cli_install_ingest.py` (extender)

**Hallazgos combinados:**

- **Claude I-D**: `install_ingest_cmd` no usa `apply_and_log` — Ctrl-C o `OSError` mid-install deja estado roto.
- **Claude/Codex paths**: `cli.py:586-592` toma `str(adapter_dir)` / `str(vault_root)` directos. Si el usuario instala con paths relativos, cron/launchd corre con CWD distinto.
- **Claude Phase-5 important #4**: `adapter_name` no se valida contra el regex de `ScheduledJob`. Spaces, `/`, unicode → plist roto.

- [ ] **Step 1: Tres tests que fallan**

```python
def test_install_ingest_resolves_paths_to_absolute(tmp_path, monkeypatch):
    # crear adapter relativo + vault relativo, invocar CLI desde tmp_path, asegurar
    # que el comando registrado en backend tiene paths absolutos.
    ...

def test_install_ingest_rolls_back_on_backend_failure(tmp_path, monkeypatch):
    # forzar backend.install a raise; verificar que el log_dir no quedó
    # y que ~/.rufino/tx/install-ingest-<job_id>.json muestra rollback completo.
    ...

def test_install_ingest_rejects_invalid_adapter_name(tmp_path):
    # adapter_name "foo/bar" o "foo bar" → exit_code != 0, mensaje claro.
    ...
```

- [ ] **Step 2: Run, fail**

- [ ] **Step 3: Fix paths**

En `cli.py:install_ingest_cmd`, antes de construir `job_id` y `cmd`:

```python
adapter_dir = adapter_dir.expanduser().resolve(strict=True)
vault_root = vault_root.expanduser().resolve(strict=True)
```

- [ ] **Step 4: Fix tx log**

Envolver el install:

```python
tx_log_path = Path.home() / ".rufino" / "tx" / f"install-ingest-{job_id}.json"
tx = TransactionLog(tx_log_path)
try:
    apply_and_log(tx, op="mkdir", target=str(log_dir),
                  apply_fn=lambda: log_dir.mkdir(parents=True, exist_ok=True),
                  rollback="rmdir_if_empty")
    apply_and_log(tx, op="plist_install", target=str(job_id),
                  apply_fn=lambda: backend.install(job_id=job_id, schedule=schedule,
                                                   cmd=cmd, log_path=str(log_path)),
                  rollback="plist_uninstall")
except Exception:
    tx.rollback()
    raise
```

- [ ] **Step 5: Validar adapter_name**

En `engine/ingest/manifest.py:14-22` o donde se valida, agregar:

```python
_ADAPTER_NAME_RE = re.compile(r"^[A-Za-z0-9._-]+$")

def parse_ingest_manifest(yaml_text: str) -> IngestAdapterManifest:
    ...
    adapter_name = raw["adapter_name"]
    if not _ADAPTER_NAME_RE.match(adapter_name):
        raise ManifestParseError(
            f"adapter_name must match {_ADAPTER_NAME_RE.pattern!r}, got {adapter_name!r}"
        )
```

- [ ] **Step 6: Correr tests**

- [ ] **Step 7: Commit**

```bash
git add src/rufino/cli.py src/rufino/engine/ingest/manifest.py tests/test_cli_install_ingest.py
git commit -m "fix(scheduler): install-ingest usa TransactionLog, resuelve paths absolutos y valida adapter_name"
```

---

## Tarea F10: `LaunchdBackend.install` — no perder schedule previo en re-install fallido

**Files:**

- Modify: `src/rufino/runtime/scheduler/launchd.py:43-58`
- Test: `tests/test_scheduler_launchd.py` (extender)

**Hallazgo (Claude I-E):** Secuencia actual: overwrite plist → bootout → bootstrap. Si `bootstrap` falla, se borra el plist nuevo pero el viejo ya fue boot-out. Resultado: el usuario pasa de "schedule corriendo" a "nada corriendo".

- [ ] **Step 1: Test que falla**

```python
def test_install_reinstall_failure_restores_prior_schedule(tmp_path):
    # 1. instalar v1 ok
    # 2. instalar v2 con bootstrap mockeado para fallar
    # 3. verificar que v1 sigue activo (o el plist restaurado)
    ...
```

- [ ] **Step 2-3: Fix**

Reordenar `LaunchdBackend.install`:

```python
def install(self, *, job_id, schedule, cmd, log_path):
    new_plist = self._plist_path(job_id)
    tmp_plist = new_plist.with_suffix(".plist.new")
    old_content = new_plist.read_text() if new_plist.exists() else None

    self._render_plist(tmp_plist, job_id=job_id, schedule=schedule, cmd=cmd, log_path=log_path)
    try:
        if old_content is not None:
            self._bootout(new_plist)
        os.replace(tmp_plist, new_plist)
        self._bootstrap(new_plist)
    except Exception:
        # restaurar
        if old_content is not None:
            new_plist.write_text(old_content)
            try: self._bootstrap(new_plist)
            except Exception: pass
        tmp_plist.unlink(missing_ok=True)
        raise
```

- [ ] **Step 4: Run, pass, commit**

```bash
git commit -m "fix(scheduler): launchd preserva schedule previo en re-install fallido"
```

---

## Tarea F11: `enable-embeddings` — vault state dentro del TransactionLog

**Files:**

- Modify: `src/rufino/cli.py` (`enable_embeddings_cmd`, `disable_embeddings_cmd`)
- Modify: `src/rufino/runtime/transaction_log.py` (registrar handlers `vault_state_write` y opcionalmente `embedding_index_rebuild`)
- Test: `tests/test_cli_enable_embeddings.py` (extender)

**Hallazgo (Claude I-F):** Si `rebuild_indices()` falla a mitad, queda sqlite poblada parcialmente y `embeddings.enabled=false`. Re-ejecutar duplica.

- [ ] **Step 1: Test que falla** — simular `rebuild` que escribe parcial y luego raise; verificar que tras rollback, `embeddings.sqlite` no quedó y `vault_state.yaml` está consistente.

- [ ] **Step 2-4: Fix** — registrar el handler `vault_state_write` con rollback `vault_state_restore_previous` (snapshot previa antes del write); envolver el flujo enable en `apply_and_log`. Si el rebuild es destructivo, snapshot el sqlite antes y restore en rollback.

- [ ] **Step 5: Commit**

```bash
git commit -m "fix(embedder): enable/disable-embeddings ahora atómicos vía TransactionLog"
```

---

## Tarea F12: `materialize --embeddings` — delegar a `enable_embeddings_cmd` o eliminar flag

**Files:**

- Modify: `src/rufino/cli.py:441-477` (`materialize_cmd`)
- Modify: `src/rufino/engine/query/semantic.py` (no crear sqlite con NoopEmbedder)
- Test: `tests/test_cli_materialize.py` (extender)

**Hallazgos:**

- **Codex P2 #2**: `materialize --embeddings` marca `enabled=true` sin `detect_ollama()` ni `rebuild_indices()`.
- **Claude Phase 2 important #4**: `SemanticBackend.__post_init__` crea `_meta/embeddings.sqlite` aunque el embedder sea `NoopEmbedder`.

- [ ] **Step 1: Tests**

```python
def test_materialize_with_embeddings_flag_runs_full_enable_flow(tmp_path, monkeypatch):
    # verificar que materialize --embeddings llama detect_ollama y rebuild_indices
    ...

def test_semantic_backend_no_sqlite_on_noop_embedder(tmp_path):
    # construir QueryLayer con NoopEmbedder → _meta/embeddings.sqlite no debe existir
    ...
```

- [ ] **Step 2-4: Decisión + fix**

Opción A (recomendada): **eliminar el flag** `--embeddings` de `materialize` y documentar el flujo en dos pasos (`materialize` → `enable-embeddings`).

Opción B: si se conserva, `materialize_cmd` debe llamar `enable_embeddings_cmd.callback(vault=vault, state_dir=state_dir)` después del materialize OK — misma atomicidad.

Para el sqlite phantom: en `SemanticBackend.__post_init__`, gateá la creación del sqlite con `isinstance(self.embedder, NoopEmbedder)` → return early, lazy.

- [ ] **Step 5: Commit**

```bash
git commit -m "fix(materialize): embeddings flag delega a enable-embeddings (o se elimina); no crear sqlite con NoopEmbedder"
```

---

## Tarea F13: Doc cleanup

**Files:**

- Modify: `CLAUDE.md` (líneas con `claude -p` y "full is stubbed")
- Modify: `docs/runtime.md:139-173` (API de scheduler obsoleta)
- Modify: `docs/primitives/query.md:54-55` (paths y claim de sqlite-vec)
- Modify: `docs/cli-reference.md` (referencias a `claude -p`)
- Modify: `src/rufino/wizard/post_bootstrap_docs.py:43` (`--vault {spec.vertical_name}` → `--vault <ruta>`)
- Modify: `src/rufino/wizard/system_prompt_assembler.py:24` (singular/plural `emit_fact`/`emit_facts`)

- [ ] **Step 1: Audit con rg**

```bash
rg -n 'claude -p|full is stubbed|ScheduledJob|sqlite-vec|--vault \{spec\.vertical_name\}|emit_fact\b' docs/ CLAUDE.md src/rufino/wizard/
```

- [ ] **Step 2: Corregir cada hit**

- `claude -p` → `claude --system-prompt ... --allowedTools ... -- "<kickoff>"` (referenciar `cli.py:407-427` para el contrato real).
- `full is stubbed (exits 2)` → eliminar o reemplazar por "single-note `--mode full` staged como tempdir-of-one y delegado a `run_batch(workers=1, batch_size=1)`".
- `ScheduledJob(..., working_dir, stdout_log, stderr_log)` → describir API v0.2.0: `SchedulerBackend.install(*, job_id, schedule, cmd, log_path)`.
- `engine/query/backends/semantic.py` → `engine/query/semantic.py`.
- "embeddings persist con `sqlite-vec`" → "embeddings se persisten como `json.dumps(vec)` en TEXT y se calcula coseno por brute force; sqlite-vec está en roadmap".
- `--vault {spec.vertical_name}` → `--vault <ruta-al-vault>` en `render_user_readme`.
- Alinear vocabulario: el wizard usa `emit_facts` (plural) y el engine `emit_fact` (singular); documentar el aliasing en `system_prompt_assembler.py` para que no sea footgun.

- [ ] **Step 3: Verificar con `rg` que no quedan hits viejos**

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md docs/ src/rufino/wizard/
git commit -m "docs: corregir referencias obsoletas (claude -p, full stubbed, sqlite-vec, ScheduledJob API)"
```

---

## Tarea F14: Bump versión a 0.2.1

**Files:**

- Modify: `src/rufino/version.py`
- Modify: `pyproject.toml`
- Modify: `migrations/README.md` (nota: v0.2.1 sin migración nueva)

- [ ] **Step 1: Bump**

```python
# src/rufino/version.py
VERSION = "0.2.1"
```

```toml
# pyproject.toml
version = "0.2.1"
```

- [ ] **Step 2: Smoke test**

```bash
pytest -q
rufino version  # debería decir 0.2.1
```

- [ ] **Step 3: Commit**

```bash
git add src/rufino/version.py pyproject.toml migrations/README.md
git commit -m "release: rufino v0.2.1 — fixes post-codereview"
```

---

## Orden de ejecución sugerido

1. **F1, F2, F3** (críticos — los 3 bloquean release; cada uno autónomo y pequeño).
2. **F6** (1-liner; desbloquea el flujo wizard guiado para embeddings).
3. **F4** (arquitectural — entrega la promesa de bounded I/O; mezclarlo con tests es la mitad del trabajo).
4. **F5** (embedder hardening — bundle: pin + catch + cache).
5. **F7 + F8** (wizard completeness — Ingest funcional + transform_hook accesible; mismo área del código).
6. **F9** (scheduler install hardening — tx log + paths + adapter_name regex).
7. **F10, F11, F12** (transactional discipline + flag cleanup).
8. **F13** (doc cleanup — barata, hacerla al final cuando todo el código está estable).
9. **F14** (bump + smoke).

Después de cada tarea: `pytest -q`. Antes del PR final: `pytest --cov=src --cov-report=term-missing` para asegurar que la cobertura no bajó.

## Gaps de test recomendados (de los reviews)

- Integración: vault materializado sin embeddings → `mcp-server --rebuild` no debe fallar (cubierto por F1).
- Integración completa `emit_facts`: `validate_spec → materialize_ingest → parse_ingest_manifest → run_ingest` (cubierto por F3).
- CLI wizard commands reales: validar que cada comando que el system prompt instruye es aceptado por Click (cubierto parcial por F6).
- Uninstall cron con ids prefijo (cubierto por F2).
- `install-ingest` con paths relativos y absolutos (cubierto por F9).
- Worker output truncation surfaced en summary (cubierto por F4).
- Hybrid mode sobre corpus vacío + corpus con contenido idéntico (recomendado tras F5).

## Lo que NO entra en este plan (queda para v0.3+)

- Multi-hop graph traversal (`depth > 1`).
- File watcher para reindex automático.
- Output adapters consumiendo `--mode semantic`.
- `sqlite-vec` para acelerar coseno.
- Minor findings de Phase-by-phase (lock para `emit_augmented`, `failed/` rename collision, `_meta/embeddings.sqlite` lazy-init más fino, etc.) — abrir issues por separado.
