# Rufino v0.2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar los 12 gaps verificados entre lo que el wizard de Rufino promete y lo que el framework entrega en v0.1.0, en una sola release big-bang v0.2.0 con embedder semántico opt-in.

**Architecture:** Big bang release. Cada fase es un bloque coherente que termina en code review + commit + push. 7 fases sobre rama `feat/v0.2.0` desde `main`. Las fases 1-6 implementan features; la fase 7 cierra con versioning, migración y test E2E.

**Tech Stack:** Python 3.11+, click para CLI, pytest + pytest-asyncio, httpx (Ollama), sentence-transformers (cross-encoder), mammoth + python-pptx (ya en pyproject.toml), flock (fcntl), launchctl/crontab (scheduler), claude CLI headless (workers).

**Spec:** [`../specs/2026-05-19-rufino-v0.2-functional-design.html`](../specs/2026-05-19-rufino-v0.2-functional-design.html)

**Workflow rules (aplican a todas las fases):**
- TDD estricto: test rojo → implementación mínima → test verde → commit.
- Cada fase termina con: `pytest` local verde → code-review agent → aplicar fixes → `pytest` verde → `git add` + `git commit` + `git push origin feat/v0.2.0`.
- Commits siguen convención: `<type>(<scope>): <description>` (feat, fix, refactor, docs, test, chore).
- Branch: `feat/v0.2.0`. Push después de cada fase.

---

## Pre-fase 0 — Setup de rama de trabajo

### Task 0.1: Crear branch `feat/v0.2.0` desde `main`

- [ ] **Step 1: Verificar main + working tree limpio**

```
git status
git branch --show-current
```

Expected: branch `main`, working tree clean.

- [ ] **Step 2: Pull último estado**

```
git pull origin main
```

- [ ] **Step 3: Crear branch nueva**

```
git checkout -b feat/v0.2.0
```

- [ ] **Step 4: Push con tracking**

```
git push -u origin feat/v0.2.0
```

---

## Fase 1 — Foundation (concurrency hardening + spec schema)

### Task 1.1: `runtime/vault_lock.py` — advisory lock per-vault

**Files:**
- Create: `src/rufino/runtime/vault_lock.py`
- Test: `tests/test_vault_lock.py`

- [ ] **Step 1: Write failing test**

`tests/test_vault_lock.py`:

```
import multiprocessing
import time
from pathlib import Path

import pytest

from rufino.runtime.vault_lock import VaultLockedError, vault_lock


def _hold_lock(vault: str, seconds: float) -> None:
    with vault_lock(Path(vault)):
        time.sleep(seconds)


def test_vault_lock_blocks_second_acquirer(tmp_path: Path) -> None:
    vault = tmp_path / "v"
    vault.mkdir()
    p = multiprocessing.Process(target=_hold_lock, args=(str(vault), 1.5))
    p.start()
    time.sleep(0.3)
    try:
        with pytest.raises(VaultLockedError):
            with vault_lock(vault, wait=False):
                pass
    finally:
        p.join()


def test_vault_lock_released_on_context_exit(tmp_path: Path) -> None:
    vault = tmp_path / "v"
    vault.mkdir()
    with vault_lock(vault):
        pass
    with vault_lock(vault):
        pass


def test_vault_lock_creates_dotrufino_dir(tmp_path: Path) -> None:
    vault = tmp_path / "v"
    vault.mkdir()
    with vault_lock(vault):
        assert (vault / ".rufino" / "lock").exists()
```

- [ ] **Step 2: Run pytest — espera FAIL**

```
pytest tests/test_vault_lock.py -v
```

- [ ] **Step 3: Implementar**

`src/rufino/runtime/vault_lock.py`:

```
import fcntl
import os
from contextlib import contextmanager
from pathlib import Path
from typing import Iterator


class VaultLockedError(RuntimeError):
    """Raised when the vault advisory lock is held by another process."""


@contextmanager
def vault_lock(vault_root: Path, *, wait: bool = False) -> Iterator[None]:
    """Acquire a flock-based advisory lock at <vault>/.rufino/lock.

    If wait=False (default), raises VaultLockedError immediately if held.
    If wait=True, blocks until acquired.
    """
    lock_dir = vault_root / ".rufino"
    lock_dir.mkdir(parents=True, exist_ok=True)
    lock_path = lock_dir / "lock"
    fd = os.open(lock_path, os.O_RDWR | os.O_CREAT, 0o600)
    try:
        flags = fcntl.LOCK_EX if wait else fcntl.LOCK_EX | fcntl.LOCK_NB
        try:
            fcntl.flock(fd, flags)
        except BlockingIOError as e:
            raise VaultLockedError(
                f"Vault {vault_root} is locked by another process. "
                f"Esperá a que termine y reintentá."
            ) from e
        try:
            yield
        finally:
            fcntl.flock(fd, fcntl.LOCK_UN)
    finally:
        os.close(fd)
```

- [ ] **Step 4: Run pytest verde**

```
pytest tests/test_vault_lock.py -v
```

- [ ] **Step 5: Stage**

```
git add src/rufino/runtime/vault_lock.py tests/test_vault_lock.py
```

### Task 1.2: Integrar `vault_lock` en `run_batch` y `resume_pending_qa`

**Files:**
- Modify: `src/rufino/engine/process/batch/runner.py`
- Modify: `src/rufino/engine/process/batch/qa_resume.py`
- Test: `tests/test_vault_lock_integration.py`

- [ ] **Step 1: Write failing test** — test que dispara dos procesos y verifica que el segundo falla con `BatchError("locked")`.

- [ ] **Step 2: Wrap `run_batch` con `vault_lock`** — al inicio de la función, envolver el cuerpo en `with vault_lock(vault_root, wait=False)`. Convertir `VaultLockedError` a `BatchError`.

- [ ] **Step 3: Mismo wrap para `resume_pending_qa`**

- [ ] **Step 4: Run pytest** — verde.

- [ ] **Step 5: Stage** — `git add` los 3 archivos.

### Task 1.3: Bounded stdout/stderr en `runner_helper.py`

**Files:**
- Modify: `src/rufino/engine/process/batch/runner_helper.py`
- Test: `tests/test_runner_output_bounded.py`

- [ ] **Step 1: Write failing test**

```
import asyncio
from pathlib import Path

import pytest

from rufino.engine.process.batch.runner_helper import MAX_OUTPUT_BYTES, run_claude_worker


@pytest.mark.asyncio
async def test_output_truncated_above_max(tmp_path: Path, monkeypatch) -> None:
    fake = tmp_path / "claude"
    fake.write_text(
        "#!/usr/bin/env python3\n"
        "import sys\n"
        "sys.stdout.write('A' * 2_000_000)\n"
        "sys.exit(0)\n"
    )
    fake.chmod(0o755)
    result = await run_claude_worker(cmd=[str(fake)], cwd=tmp_path, timeout=10.0)
    assert len(result.stdout) <= MAX_OUTPUT_BYTES
    assert result.truncated is True
```

- [ ] **Step 2: Run pytest — FAIL**

- [ ] **Step 3: Implement bounded read**

Agregar a `runner_helper.py`:

```
import asyncio
from dataclasses import dataclass

MAX_OUTPUT_BYTES = 1_000_000


@dataclass(frozen=True)
class WorkerResult:
    returncode: int
    stdout: bytes
    stderr: bytes
    truncated: bool = False
    timed_out: bool = False


async def _read_bounded(stream: asyncio.StreamReader, cap: int):
    buf = bytearray()
    truncated = False
    while True:
        chunk = await stream.read(4096)
        if not chunk:
            break
        if len(buf) + len(chunk) > cap:
            buf.extend(chunk[: cap - len(buf)])
            truncated = True
            break
        buf.extend(chunk)
    return bytes(buf), truncated


async def run_claude_worker(*, cmd, cwd: Path, timeout: float) -> WorkerResult:
    proc = await asyncio.create_subprocess_exec(
        *cmd, cwd=str(cwd),
        stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.PIPE,
    )
    try:
        stdout, out_trunc = await asyncio.wait_for(
            _read_bounded(proc.stdout, MAX_OUTPUT_BYTES), timeout=timeout
        )
        stderr, err_trunc = await asyncio.wait_for(
            _read_bounded(proc.stderr, MAX_OUTPUT_BYTES), timeout=timeout
        )
        await proc.wait()
        return WorkerResult(proc.returncode, stdout, stderr, out_trunc or err_trunc)
    except asyncio.TimeoutError:
        proc.kill()
        await proc.wait()
        return WorkerResult(-1, b"", b"", False, timed_out=True)
```

- [ ] **Step 4: Run pytest verde**

- [ ] **Step 5: Stage**

```
git add src/rufino/engine/process/batch/runner_helper.py tests/test_runner_output_bounded.py
```

### Task 1.4: Worker ID padding `:04d`

**Files:**
- Modify: `src/rufino/engine/process/batch/planner.py`
- Test: `tests/test_planner_worker_id_padding.py`

- [ ] **Step 1: Write failing test** — corpus de 1500 notas, esperando IDs `w0001..w0150` con longitud 5.

- [ ] **Step 2: Cambiar `:03d` por `:04d`** en `planner.py`.

- [ ] **Step 3: Update tests existentes** — `grep -rn 'w001\|w002' tests/` y reemplazar por `w0001`, `w0002`.

- [ ] **Step 4: `pytest` full — verde sin regresión.**

- [ ] **Step 5: Stage** — los 2 archivos + tests actualizados.

### Task 1.5: Formalizar `WizardSpec` con dataclasses tipados

**Files:**
- Modify: `src/rufino/wizard/spec_schema.py`
- Test: `tests/test_spec_schema_v0_2.py`

- [ ] **Step 1: Write failing test**

```
import pytest

from rufino.wizard.spec_schema import (
    IngestSpec, ProcessSpec, OutputSpec, SpecError, validate_spec,
)


def _base_raw():
    return {
        "vertical_name": "facultad",
        "patterns": ["long_documents_extraction"],
        "entities": ["apunte_clase"],
        "vocabulary": {"apunte_clase": "apuntes/<materia>/<slug>.md"},
        "sources": [], "processing": [], "outputs": [],
    }


def test_validate_spec_accepts_typed_sources():
    raw = _base_raw()
    raw["processing"] = [{
        "adapter_name": "apunte-clase", "note_type": "apunte_clase",
        "applies_when": {"source_dir": "inbox/"}, "llm": "sonnet",
        "output_schema": {"required": {"title": "string"}, "optional": {}},
        "triple_vocabulary": ["tema-de"],
        "tag_axes": [{"axis": "materia", "format": "materia/<slug>", "required": True, "min": 1}],
        "destination_path": "apuntes/{materia}/{slug}.md",
        "qa_triggers": [], "context_injectors": [],
        "batch_size": 10,
        "prompt_instructions": "# Procesá apuntes\n",
    }]
    spec = validate_spec(raw)
    assert isinstance(spec.processing[0], ProcessSpec)
    assert spec.processing[0].prompt_instructions.startswith("# Procesá")


def test_validate_spec_rejects_processing_without_prompt_instructions():
    raw = _base_raw()
    raw["processing"] = [{
        "adapter_name": "x", "note_type": "x", "applies_when": {}, "llm": "sonnet",
        "output_schema": {"required": {}, "optional": {}},
        "triple_vocabulary": [], "tag_axes": [],
        "destination_path": "x.md", "qa_triggers": [], "context_injectors": [],
        "batch_size": 10,
    }]
    with pytest.raises(SpecError, match="prompt_instructions"):
        validate_spec(raw)


def test_validate_spec_rejects_cross_ref_missing_process():
    raw = _base_raw()
    raw["sources"] = [{
        "adapter_name": "x", "source_name": "x", "output_mode": "import_raw",
        "schedule": None, "auth": {"type": "none"},
        "target_inbox": "inbox/", "process_with": "no-existe", "trigger": "immediate",
    }]
    with pytest.raises(SpecError, match="process_with.*no-existe"):
        validate_spec(raw)
```

- [ ] **Step 2: Implement `IngestSpec` / `ProcessSpec` / `OutputSpec`** como dataclasses frozen + validadores `_validate_ingest`, `_validate_process`, `_validate_output`. Cross-ref check entre `sources[].process_with` y `processing[].adapter_name`.

- [ ] **Step 3: Update tests existentes** que asumen el shape libre — actualizar fixtures al schema nuevo.

- [ ] **Step 4: `pytest` verde.**

- [ ] **Step 5: Stage.**

### Task 1.6: Fin de fase 1 — Code review + commit + push

- [ ] **Step 1: `pytest` full verde.**

- [ ] **Step 2: Dispatch code-reviewer agent.**

Llamar `pr-review-toolkit:code-reviewer` con prompt: "Revisá el diff staged contra main. Foco: corrección, atomicidad, manejo de errores en vault_lock, bounded I/O, padding worker ID, schema spec con dataclasses frozen."

- [ ] **Step 3: Aplicar fixes** que el agent reporta.

- [ ] **Step 4: Commit fase 1**

```
git commit -m "feat(v0.2): foundation — vault_lock, bounded I/O, worker padding, spec schema

- runtime/vault_lock.py: flock-based advisory lock per-vault
- engine/process/batch/runner_helper.py: bounded stdout/stderr (1MB cap)
- engine/process/batch/planner.py: worker ID padding :04d
- wizard/spec_schema.py: IngestSpec/ProcessSpec/OutputSpec typed dataclasses
- Integration: run_batch + resume_pending_qa acquire vault_lock"
```

- [ ] **Step 5: Push**

```
git push origin feat/v0.2.0
```

---

## Fase 2 — Wizard end-to-end (gaps 1, 2, 3)

### Task 2.1: `wizard/adapter_materializers/ingest.py`

**Files:**
- Create: `src/rufino/wizard/adapter_materializers/__init__.py` (vacío)
- Create: `src/rufino/wizard/adapter_materializers/ingest.py`
- Test: `tests/test_materialize_ingest_adapter.py`

- [ ] **Step 1: Write failing test**

```
from pathlib import Path
import yaml
import pytest

from rufino.wizard.spec_schema import IngestSpec
from rufino.wizard.adapter_materializers.ingest import materialize_ingest
from rufino.runtime.transaction_log import TransactionLog


def _spec(**overrides) -> IngestSpec:
    base = dict(
        adapter_name="drive-facultad", source_name="gdrive",
        output_mode="import_raw", schedule="*/30 * * * *",
        auth={"type": "oauth2", "keychain_service": "rufino-gdrive"},
        target_inbox="inbox/cufona/", process_with="apunte-clase",
        trigger="immediate",
    )
    base.update(overrides)
    return IngestSpec(**base)


def test_materialize_ingest_creates_manifest(tmp_path: Path) -> None:
    log = TransactionLog(tmp_path / "tx.json")
    out = materialize_ingest(
        spec=_spec(), base_dir=tmp_path / "rufino", vault_slug="facultad", tx_log=log,
    )
    assert (out / "manifest.yaml").exists()
    parsed = yaml.safe_load((out / "manifest.yaml").read_text())
    assert parsed["adapter_name"] == "drive-facultad"
```

- [ ] **Step 2: Run — FAIL.**

- [ ] **Step 3: Implement**

```
# src/rufino/wizard/adapter_materializers/__init__.py
# (empty)
```

```
# src/rufino/wizard/adapter_materializers/ingest.py
from pathlib import Path
import yaml

from rufino.engine.ingest.manifest import parse_ingest_manifest, ManifestError
from rufino.runtime.transaction_log import TransactionLog, apply_and_log
from rufino.wizard.spec_schema import IngestSpec


def materialize_ingest(*, spec: IngestSpec, base_dir: Path, vault_slug: str, tx_log: TransactionLog) -> Path:
    adapter_dir = base_dir / "adapters" / "ingest" / vault_slug / spec.adapter_name
    apply_and_log(
        tx_log, op="mkdir", target=str(adapter_dir),
        apply_fn=lambda: adapter_dir.mkdir(parents=True),
        rollback="rmdir",
    )
    manifest = _spec_to_manifest_dict(spec)
    manifest_yaml = yaml.safe_dump(manifest, sort_keys=False, allow_unicode=True)
    try:
        parse_ingest_manifest(manifest_yaml)
    except ManifestError as e:
        raise ValueError(f"Ingest adapter {spec.adapter_name} invalid: {e}") from e
    manifest_path = adapter_dir / "manifest.yaml"
    apply_and_log(
        tx_log, op="write", target=str(manifest_path),
        apply_fn=lambda: manifest_path.write_text(manifest_yaml, encoding="utf-8"),
        rollback="delete",
    )
    return adapter_dir


def _spec_to_manifest_dict(spec: IngestSpec) -> dict:
    d = {
        "adapter_name": spec.adapter_name,
        "source_name": spec.source_name,
        "output_mode": spec.output_mode,
        "auth": dict(spec.auth),
    }
    if spec.schedule:
        d["schedule"] = spec.schedule
    for field in ("emits", "fact_schema", "destination", "dedup_by",
                  "target_inbox", "process_with", "trigger", "process_inline_with"):
        v = getattr(spec, field)
        if v is not None:
            d[field] = list(v) if isinstance(v, tuple) else (dict(v) if hasattr(v, "items") else v)
    return d
```

- [ ] **Step 4: `pytest` verde.**

- [ ] **Step 5: Stage.**

### Task 2.2: `wizard/adapter_materializers/process.py`

Análogo a 2.1 pero escribe también `prompt.md` desde `spec.prompt_instructions`. Test verifica que ambos archivos existen y que el contenido del `prompt.md` coincide.

- [ ] **Step 1-5: TDD como 2.1.**

```
# src/rufino/wizard/adapter_materializers/process.py
from pathlib import Path
import yaml

from rufino.engine.process.manifest import parse_process_manifest, ManifestError
from rufino.runtime.transaction_log import TransactionLog, apply_and_log
from rufino.wizard.spec_schema import ProcessSpec


def materialize_process(*, spec: ProcessSpec, base_dir: Path, vault_slug: str, tx_log: TransactionLog) -> Path:
    adapter_dir = base_dir / "adapters" / "process" / vault_slug / spec.adapter_name
    apply_and_log(
        tx_log, op="mkdir", target=str(adapter_dir),
        apply_fn=lambda: adapter_dir.mkdir(parents=True),
        rollback="rmdir",
    )
    manifest_yaml = yaml.safe_dump(_spec_to_manifest_dict(spec), sort_keys=False, allow_unicode=True)
    try:
        parse_process_manifest(manifest_yaml)
    except ManifestError as e:
        raise ValueError(f"Process adapter {spec.adapter_name} invalid: {e}") from e
    manifest_path = adapter_dir / "manifest.yaml"
    apply_and_log(
        tx_log, op="write", target=str(manifest_path),
        apply_fn=lambda: manifest_path.write_text(manifest_yaml, encoding="utf-8"),
        rollback="delete",
    )
    prompt_path = adapter_dir / "prompt.md"
    apply_and_log(
        tx_log, op="write", target=str(prompt_path),
        apply_fn=lambda: prompt_path.write_text(spec.prompt_instructions, encoding="utf-8"),
        rollback="delete",
    )
    return adapter_dir


def _spec_to_manifest_dict(spec: ProcessSpec) -> dict:
    return {
        "adapter_name": spec.adapter_name,
        "note_type": spec.note_type,
        "applies_when": dict(spec.applies_when),
        "llm": spec.llm,
        "output_schema": {
            "required": dict(spec.output_schema.get("required", {})),
            "optional": dict(spec.output_schema.get("optional", {})),
        },
        "triple_vocabulary": list(spec.triple_vocabulary),
        "tag_axes": [dict(a) for a in spec.tag_axes],
        "destination_path": spec.destination_path,
        "qa_triggers": [dict(q) for q in spec.qa_triggers],
        "context_injectors": [dict(c) for c in spec.context_injectors],
        "batch_size": spec.batch_size,
    }
```

### Task 2.3: `wizard/adapter_materializers/output.py`

Análogo: escribe `manifest.yaml` + `template.md` desde `spec.template_body`. Mismo patrón de validación contra `parse_output_manifest`.

### Task 2.4: `materializer.py` itera spec.sources/processing/outputs

**Files:**
- Modify: `src/rufino/wizard/materializer.py`
- Test: `tests/test_materialize_full.py`

- [ ] **Step 1: Write failing test** que crea spec con 1 source + 1 process + 1 output, verifica que post-materialize existen los 3 archivos manifest + prompt + template + memory_loop adapter + vault skeleton.

- [ ] **Step 2: Modify `materialize()`** — agregar el bloque de iteración:

```
from rufino.wizard.adapter_materializers.ingest import materialize_ingest
from rufino.wizard.adapter_materializers.process import materialize_process
from rufino.wizard.adapter_materializers.output import materialize_output

base_dir = state_dir.parent
for ingest_spec in spec.sources:
    materialize_ingest(spec=ingest_spec, base_dir=base_dir, vault_slug=slug, tx_log=tx_log)
for process_spec in spec.processing:
    materialize_process(spec=process_spec, base_dir=base_dir, vault_slug=slug, tx_log=tx_log)
for output_spec in spec.outputs:
    materialize_output(spec=output_spec, base_dir=base_dir, vault_slug=slug, tx_log=tx_log)
```

- [ ] **Step 3: Update docstring** — reemplazar el viejo "v1 wires only the Memory loop installer..." con uno actualizado.

- [ ] **Step 4: `pytest` verde.**

- [ ] **Step 5: Stage.**

### Task 2.5: Update wizard system prompt

**Files:**
- Modify: `src/rufino/wizard/system_prompt_assembler.py` (template)
- Modify: `src/rufino/wizard/operative_rules.md`
- Modify: `src/rufino/wizard/checklist.md`
- Test: `tests/test_wizard_prompt_v0_2.py`

- [ ] **Step 1: Test** — el prompt mencione "prompt_instructions", "template_body", "corpus", "embeddings".

- [ ] **Step 2: Update `_TEMPLATE`** en `system_prompt_assembler.py` agregando bloque "Output esperado (detallado)" que pide `prompt_instructions` por cada processing y `template_body` por cada output.

- [ ] **Step 3: Update `operative_rules.md`** agregando reglas:
  - **#9** Preguntar por embeddings antes del big bang, con `--embeddings/--no-embeddings`
  - **#10** Después del materialize, preguntar por corpus inicial e invocar `process-batch` si aplica
  - **#11** Calidad del `prompt_instructions` (auto-contenido, operativo, con ejemplos)
  - **#12** Schedules de ingest: preguntar e invocar `install-ingest` si aplica

- [ ] **Step 4: Update `checklist.md`** agregando los nuevos ítems (embeddings decidido, prompt_instructions redactadas, template_body redactado).

- [ ] **Step 5: `pytest` verde.**

- [ ] **Step 6: Stage.**

### Task 2.6: Update `--allowedTools` en `bootstrap_cmd`

Extender el string de `--allowedTools` en `src/rufino/cli.py:bootstrap_cmd` agregando: `Bash(rufino process-batch:*),Bash(rufino detect-embeddings:*),Bash(rufino enable-embeddings:*),Bash(rufino install-ingest:*)`.

### Task 2.7: Fin de fase 2 — Code review + commit + push

- [ ] **Step 1: `pytest` full verde.**
- [ ] **Step 2: Code-reviewer agent** — foco en adapter materializers + atomicidad del rollback + serialización manifests.
- [ ] **Step 3: Aplicar fixes.**
- [ ] **Step 4: Commit fase 2** con mensaje `feat(v0.2): wizard end-to-end — adapter materializers + prompt updates`.
- [ ] **Step 5: Push.**

---

## Fase 3 — Embedder opt-in (gap 4)

### Task 3.1: `runtime/embedder/detect.py` + CLI `detect-embeddings`

**Files:**
- Create: `src/rufino/runtime/embedder/__init__.py` (vacío)
- Create: `src/rufino/runtime/embedder/detect.py`
- Modify: `src/rufino/cli.py`
- Test: `tests/test_embedder_detect.py`

- [ ] **Step 1: Write failing test**

```
from unittest.mock import patch, MagicMock

from rufino.runtime.embedder.detect import detect_ollama


def test_detect_all_present():
    with patch("shutil.which", return_value="/usr/local/bin/ollama"), \
         patch("httpx.get") as mget:
        mget.return_value = MagicMock(status_code=200, json=lambda: {
            "models": [{"name": "nomic-embed-text:latest"}],
        })
        r = detect_ollama()
    assert r.binary_present and r.server_running and r.model_installed
    assert r.error is None


def test_detect_no_binary():
    with patch("shutil.which", return_value=None):
        r = detect_ollama()
    assert not r.binary_present
```

- [ ] **Step 2: Implement**

```
# src/rufino/runtime/embedder/detect.py
import shutil
from dataclasses import dataclass

import httpx


@dataclass(frozen=True)
class OllamaDetection:
    binary_present: bool
    server_running: bool
    model_installed: bool
    error: str | None


def detect_ollama(*, base_url: str = "http://localhost:11434", model: str = "nomic-embed-text") -> OllamaDetection:
    if shutil.which("ollama") is None:
        return OllamaDetection(False, False, False, "ollama binary not in PATH")
    try:
        resp = httpx.get(f"{base_url}/api/tags", timeout=3.0)
        resp.raise_for_status()
    except Exception as e:
        return OllamaDetection(True, False, False, f"ollama server not responding: {e}")
    models = [m["name"] for m in resp.json().get("models", [])]
    found = any(m.startswith(model) for m in models)
    return OllamaDetection(True, True, found, None if found else f"model {model!r} not pulled")
```

- [ ] **Step 3: Add CLI command** en `cli.py`:

```
@cli.command(name="detect-embeddings")
def detect_embeddings_cmd():
    from rufino.runtime.embedder.detect import detect_ollama
    r = detect_ollama()
    if r.binary_present and r.server_running and r.model_installed:
        click.echo("OK: ollama + nomic-embed-text ready")
        return
    click.echo(f"NOT READY: {r.error}", err=True)
    raise click.exceptions.Exit(code=1)
```

- [ ] **Step 4: `pytest` verde.**

- [ ] **Step 5: Stage.**

### Task 3.2: `runtime/embedder/ollama.py` — `OllamaEmbedder`

**Files:**
- Create: `src/rufino/runtime/embedder/ollama.py`
- Test: `tests/test_embedder_ollama.py`

```
# src/rufino/runtime/embedder/ollama.py
from dataclasses import dataclass

import httpx


@dataclass(frozen=True)
class OllamaEmbedder:
    model: str = "nomic-embed-text"
    base_url: str = "http://localhost:11434"
    timeout: float = 30.0

    def embed(self, text: str) -> list[float]:
        resp = httpx.post(
            f"{self.base_url}/api/embeddings",
            json={"model": self.model, "prompt": text},
            timeout=self.timeout,
        )
        resp.raise_for_status()
        return resp.json()["embedding"]
```

Test: mock httpx.post + happy path + timeout + 500 error.

### Task 3.3: `runtime/embedder/cross_encoder.py` — re-ranker

**Files:**
- Create: `src/rufino/runtime/embedder/cross_encoder.py`
- Test: `tests/test_embedder_cross_encoder.py`

```
# src/rufino/runtime/embedder/cross_encoder.py
from dataclasses import dataclass, field
from typing import Optional


@dataclass
class CrossEncoderReranker:
    model_name: str = "BAAI/bge-reranker-base"
    _model: Optional[object] = field(default=None, init=False, repr=False)

    def _load_model(self):
        if self._model is None:
            from sentence_transformers import CrossEncoder
            self._model = CrossEncoder(self.model_name)
        return self._model

    def rerank(self, query: str, candidates: list[str]) -> list[str]:
        if not candidates:
            return []
        model = self._load_model()
        pairs = [(query, c) for c in candidates]
        scores = model.predict(pairs)
        ranked = sorted(zip(candidates, scores), key=lambda x: x[1], reverse=True)
        return [c for c, _ in ranked]
```

Test: mock `_load_model` returning fake model con predict mocked; verifica orden.

`pyproject.toml`: agregar `sentence-transformers>=3.0` y `httpx>=0.27` en dependencies.

### Task 3.4: `runtime/embedder/resolve.py` + per-vault yaml

**Files:**
- Create: `src/rufino/runtime/embedder/resolve.py`
- Test: `tests/test_resolve_embedder.py`

```
# src/rufino/runtime/embedder/resolve.py
import os
import tempfile
from dataclasses import dataclass
from pathlib import Path

import yaml

from rufino.runtime.embedder.ollama import OllamaEmbedder


@dataclass(frozen=True)
class NoopEmbedder:
    def embed(self, text: str) -> list[float]:
        raise NotImplementedError(
            "embeddings no configurados para este vault; "
            "corré `rufino enable-embeddings --vault X`"
        )


def _state_path(vault_slug: str, state_dir: Path) -> Path:
    return state_dir / "vaults" / f"{vault_slug}.yaml"


def resolve_embedder(*, vault_slug: str, state_dir: Path):
    p = _state_path(vault_slug, state_dir)
    if not p.exists():
        return NoopEmbedder()
    try:
        data = yaml.safe_load(p.read_text(encoding="utf-8")) or {}
    except yaml.YAMLError as e:
        raise RuntimeError(f"vault state corrupted at {p}: {e}") from e
    emb = data.get("embeddings", {})
    if not emb.get("enabled"):
        return NoopEmbedder()
    backend = emb.get("backend", "ollama")
    if backend == "ollama":
        return OllamaEmbedder(model=emb.get("model", "nomic-embed-text"))
    raise RuntimeError(f"unknown embedder backend {backend!r}")


def write_vault_state(*, vault_slug: str, state_dir: Path, embeddings_enabled: bool,
                      backend: str = "ollama", model: str = "nomic-embed-text") -> None:
    target = _state_path(vault_slug, state_dir)
    target.parent.mkdir(parents=True, exist_ok=True)
    payload = {
        "vault_slug": vault_slug,
        "embeddings": {"enabled": embeddings_enabled, "backend": backend, "model": model},
    }
    fd, tmp = tempfile.mkstemp(dir=target.parent, suffix=".yaml")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            yaml.safe_dump(payload, f, sort_keys=False, allow_unicode=True)
        os.replace(tmp, target)
    except Exception:
        try: os.unlink(tmp)
        except OSError: pass
        raise
```

Tests: yaml presente true → Ollama; false → Noop; missing → Noop; corrupt → RuntimeError. Y `write_vault_state` atómico.

### Task 3.5: CLI `enable-embeddings` / `disable-embeddings`

**Files:**
- Modify: `src/rufino/cli.py`
- Test: `tests/test_cli_enable_disable_embeddings.py`

Agregar dos commands. `enable` corre `detect_ollama()` primero, escribe state yaml + corre `QueryLayer(...).rebuild_indices()`. `disable` solo escribe `enabled: false` (idempotente, no borra `embeddings.sqlite`).

### Task 3.6: Integrar `_resolve_embedder` en `cli.py`

- Eliminar `_NoopEmbeddings` local. Importar `NoopEmbedder` y `resolve_embedder` desde `runtime/embedder/resolve.py`.
- `query_cmd`, `mcp_server_cmd`, `_LexicalQueryAdapter`: agregar `--state-dir` opcional (default `~/.rufino/state`), usar `resolve_embedder(vault_slug=compute_vault_slug(vault_root), state_dir=...)`.
- `query --mode semantic` o `hybrid` con NoopEmbedder → exit 2 con mensaje específico.
- `materialize_cmd`: nuevo flag `--embeddings/--no-embeddings`; al cierre, `write_vault_state(...)`.

### Task 3.7: Hybrid mode con cross-encoder real

**Files:**
- Modify: `src/rufino/engine/query/api.py`
- Test: `tests/test_query_hybrid_with_rerank.py`

```
def search(self, query: str, *, mode: str = "hybrid", k: int = 10):
    if mode == "lexical":
        return self._lexical_search(query, k=k)
    if mode == "semantic":
        return self._semantic_search(query, k=k)
    if mode == "hybrid":
        from rufino.runtime.embedder.resolve import NoopEmbedder
        if isinstance(self._embedder, NoopEmbedder):
            raise NotImplementedError(
                "embeddings no configurados para este vault; "
                "corré `rufino enable-embeddings --vault X`"
            )
        lex = self._lexical_search(query, k=50)
        sem = self._semantic_search(query, k=50)
        seen = set()
        union = []
        for n in lex + sem:
            if n.relative_path not in seen:
                seen.add(n.relative_path); union.append(n)
        from rufino.runtime.embedder.cross_encoder import CrossEncoderReranker
        rer = CrossEncoderReranker()
        candidates = [(self.vault_root / n.relative_path).read_text(encoding="utf-8", errors="replace") for n in union]
        path_by_content = {c: n.relative_path for c, n in zip(candidates, union)}
        ordered = rer.rerank(query, candidates)
        return [next(n for n in union if n.relative_path == path_by_content[c]) for c in ordered][:k]
    raise ValueError(f"unknown mode {mode!r}")
```

Test: mock lexical/semantic searches + mock CrossEncoderReranker.rerank → verifica que el orden devuelto coincide con el del re-rank.

### Task 3.8: Fin de fase 3 — Code review + commit + push

- [ ] `pytest` verde
- [ ] Code-reviewer agent — foco en resolve_embedder, atomicidad del state yaml, behavior del hybrid mode sin embeddings
- [ ] Fixes
- [ ] Commit `feat(v0.2): embedder opt-in — Ollama + cross-encoder hybrid`
- [ ] Push

---

## Fase 4 — Worker primitives diferidas (gaps 6, 7, 8)

### Task 4.1: `runtime/transform_hook.py`

**Files:**
- Create: `src/rufino/runtime/transform_hook.py`
- Test: `tests/test_transform_hook.py`

```
# src/rufino/runtime/transform_hook.py
import json
import subprocess
import sys
from pathlib import Path
from typing import Any


class TransformHookError(RuntimeError):
    """Raised when a transform_hook fails (timeout, bad exit, malformed output)."""


def run_transform_hook(script_path: Path, input_dict: dict[str, Any], *, timeout: float = 30.0) -> dict[str, Any]:
    """Run a Python transform_hook in subprocess sandbox.

    Input via stdin (JSON), output via stdout (JSON). Raises TransformHookError on
    timeout, non-zero exit, or malformed output. Caller treats those as warnings
    (no abort) per the spec.
    """
    payload = json.dumps(input_dict).encode("utf-8")
    try:
        result = subprocess.run(
            [sys.executable, str(script_path)],
            input=payload, capture_output=True, timeout=timeout,
        )
    except subprocess.TimeoutExpired as e:
        raise TransformHookError(f"transform_hook {script_path} timeout") from e
    if result.returncode != 0:
        raise TransformHookError(
            f"transform_hook {script_path} exited {result.returncode}: "
            f"{result.stderr.decode('utf-8', errors='replace')[:500]}"
        )
    try:
        return json.loads(result.stdout)
    except json.JSONDecodeError as e:
        raise TransformHookError(f"transform_hook {script_path} malformed JSON: {e}") from e
```

Tests:
- happy path: hook modifica el dict y devuelve
- timeout: TransformHookError
- syntax error: TransformHookError
- (red bloqueado es defense-in-depth, no se testea explícitamente)

### Task 4.2: Invocar `transform_hook` desde Ingest y Process

**Files:**
- Modify: `src/rufino/engine/ingest/runner.py`
- Modify: `src/rufino/engine/process/batch/committer.py` (o donde se renderiza el output)
- Test: `tests/test_transform_hook_integration.py`

Agregar helper `_maybe_apply_transform_hook(hook_path, record)` que aplica el hook si existe; si tira `TransformHookError`, loggea warning y devuelve el record original (graceful degrade).

Llamarlo en `_run_emit_fact`, `_run_import_raw` (después del fetch, antes del write) y en el Process post-LLM call.

### Task 4.3: Ingest `emit_augmented` dispatcher

**Files:**
- Create: `src/rufino/engine/ingest/emit_augmented.py`
- Modify: `src/rufino/engine/ingest/runner.py` (rama `emit_augmented`)
- Test: `tests/test_ingest_emit_augmented.py`

```
# src/rufino/engine/ingest/emit_augmented.py
import logging
from pathlib import Path

logger = logging.getLogger(__name__)


def dispatch_to_process(*, record: dict, process_adapter_dir: Path, vault_root: Path, staging_dir: Path) -> dict:
    """Send a single record directly to a Process adapter, bypassing inbox."""
    from rufino.engine.process.dispatcher import process_note
    staging_dir.mkdir(parents=True, exist_ok=True)
    note_path = staging_dir / f"{record.get('id', 'unknown')}.md"
    note_path.write_text(record.get("content") or str(record), encoding="utf-8")
    try:
        result = process_note(note_path=note_path, vault_root=vault_root, mode="light")
        return {"status": "ok", "message": result.message}
    except Exception as e:
        failed = staging_dir / "failed"
        failed.mkdir(exist_ok=True)
        note_path.rename(failed / note_path.name)
        logger.error(f"emit_augmented Process failed for {record.get('id')}: {e}")
        return {"status": "failed", "error": str(e)}
```

En `runner.py:58`, reemplazar el `raise NotImplementedError` con:

```
if manifest.output_mode == "emit_augmented":
    from rufino.engine.ingest.emit_augmented import _run_emit_augmented
    return _run_emit_augmented(adapter_dir=adapter_dir, manifest=manifest,
                                vault_root=vault_root, rufino_state_dir=rufino_state_dir)
```

Y agregar `_run_emit_augmented` que itera el fetcher e invoca `dispatch_to_process`.

### Task 4.4: Single-note `process --mode full` (wrapper sobre batch)

**Files:**
- Modify: `src/rufino/cli.py` (`process_cmd` rama `full`)
- Test: `tests/test_process_single_full.py`

Reemplazar el `exit 2` actual en `process_cmd` con un wrapper que:
1. Crea tempdir con la nota
2. Llama `run_batch(source=tmpdir, workers=1, batch_size=1, ...)`
3. Si `notes_pending_qa > 0` → exit 3 (Q&A pendiente)
4. Si `notes_failed > 0` → exit 1 (staging preserved)
5. Sino → exit 0

### Task 4.5: Fin de fase 4 — Code review + commit + push

- [ ] `pytest` verde
- [ ] Code-reviewer agent — foco en sandboxing del hook, graceful degradation, idempotencia del wrapper
- [ ] Fixes
- [ ] Commit `feat(v0.2): worker primitives — transform_hook, emit_augmented, mode=full`
- [ ] Push

---

## Fase 5 — Query enriquecida (gap 5)

### Task 5.1: Forward graph traversal

**Files:**
- Modify: `src/rufino/engine/query/graph.py`
- Test: `tests/test_query_graph_forward.py`

En `graph.py:71`, reemplazar el `NotImplementedError` con la rama forward:

```
def traverse(self, *, node: str, relation: str, depth: int = 1, reverse: bool = False):
    if depth != 1:
        raise NotImplementedError("multi-hop traversal (depth > 1) deferred to v1.1")
    if reverse:
        cur = self._conn.execute("SELECT s FROM triples WHERE r=? AND o=?", (relation, node))
    else:
        cur = self._conn.execute("SELECT o FROM triples WHERE r=? AND s=?", (relation, node))
    return [r[0] for r in cur.fetchall()]
```

Tests: forward depth=1 OK; depth>1 raisea.

### Task 5.2: MCP `traverse_relations` soporta `reverse=false`

Revisar el schema del tool en `mcp_server/server.py`: asegurar que acepta `reverse: bool` con default false, y que invoca el engine pasando el flag.

### Task 5.3: Fin de fase 5 — Code review + commit + push

- [ ] `pytest` verde
- [ ] Code-reviewer agent
- [ ] Fixes
- [ ] Commit `feat(v0.2): forward graph traversal — depth=1`
- [ ] Push

---

## Fase 6 — Scheduler real (gap 9)

### Task 6.1: `runtime/scheduler/__init__.py`

**Files:**
- Create: `src/rufino/runtime/scheduler/__init__.py`
- Test: `tests/test_scheduler_init.py`

```
# src/rufino/runtime/scheduler/__init__.py
import platform


def get_backend():
    system = platform.system()
    if system == "Darwin":
        from rufino.runtime.scheduler.launchd import LaunchdBackend
        return LaunchdBackend()
    if system == "Linux":
        from rufino.runtime.scheduler.cron import CronBackend
        return CronBackend()
    raise NotImplementedError(f"No scheduler backend for {system!r}")


def validate_cron(expr: str) -> None:
    parts = expr.strip().split()
    if len(parts) != 5:
        raise ValueError(f"Cron must have 5 fields, got {len(parts)}: {expr!r}")
    ranges = [(0, 59), (0, 23), (1, 31), (1, 12), (0, 6)]
    for i, (part, (lo, hi)) in enumerate(zip(parts, ranges)):
        if part == "*": continue
        if part.startswith("*/"):
            try: int(part[2:])
            except ValueError: raise ValueError(f"Invalid step in field {i}: {part!r}")
            continue
        try:
            v = int(part)
            if not (lo <= v <= hi): raise ValueError(f"Field {i} value {v} out of range [{lo},{hi}]")
        except ValueError as e:
            raise ValueError(f"Invalid cron field {i}: {part!r}") from e
```

### Task 6.2: `runtime/scheduler/launchd.py`

**Files:**
- Create: `src/rufino/runtime/scheduler/launchd.py`
- Test: `tests/test_scheduler_launchd.py`

Implementa `LaunchdBackend(launchagents_dir=Path)` con métodos `install(job_id, schedule, cmd, log_path)`, `uninstall(job_id)`, `list_jobs()`. El plist se construye con `_build_plist()` que mapea cron a `StartCalendarInterval`. `launchctl bootstrap gui/<uid> <plist>` y `launchctl bootout`. Tests con mock subprocess.

### Task 6.3: `runtime/scheduler/cron.py`

**Files:**
- Create: `src/rufino/runtime/scheduler/cron.py`
- Test: `tests/test_scheduler_cron.py`

Implementa `CronBackend` con methods igual signature que launchd. Read/write crontab via `subprocess.run(["crontab", "-l"])` + `subprocess.run(["crontab", "-"], input=new)`. Cada entry marcada con `# <job_id>` al final para edit/uninstall sin tocar otras entries.

### Task 6.4: CLI `install-ingest` / `uninstall-ingest` / `list-ingests`

**Files:**
- Modify: `src/rufino/cli.py`
- Test: `tests/test_cli_scheduler.py`

Tres commands nuevos. `install-ingest`: lee el manifest del adapter, valida que tenga `schedule`, computa `job_id = rufino-ingest-<vault-slug>-<adapter-name>`, invoca `backend.install(...)`. Logs van a `~/.rufino/logs/<job_id>.log`.

### Task 6.5: Fin de fase 6 — Code review + commit + push

- [ ] `pytest` verde
- [ ] Code-reviewer agent — foco en validación de cron, marker isolation en crontab, idempotencia
- [ ] Fixes
- [ ] Commit `feat(v0.2): scheduler launchd/cron + ingest install commands`
- [ ] Push

---

## Fase 7 — Release (versioning + migración + E2E + docs)

### Task 7.1: Version bump

- `src/rufino/version.py`: `VERSION = "0.2.0"`
- `pyproject.toml`: `version = "0.2.0"`

### Task 7.2: Migration `migrations/0.1.0-to-0.2.0.sh`

**Files:**
- Create: `migrations/0.1.0-to-0.2.0.sh` (chmod +x)

```
#!/usr/bin/env bash
set -euo pipefail
RUFINO_HOME="${RUFINO_HOME:-$HOME/.rufino}"
STATE_VAULTS_DIR="$RUFINO_HOME/state/vaults"
mkdir -p "$STATE_VAULTS_DIR"

if [ -d "$RUFINO_HOME/adapters/memory_loop" ]; then
  for adapter_dir in "$RUFINO_HOME/adapters/memory_loop"/*/; do
    slug=$(basename "$adapter_dir")
    yaml_path="$STATE_VAULTS_DIR/$slug.yaml"
    if [ ! -f "$yaml_path" ]; then
      cat > "$yaml_path" <<INNER
vault_slug: $slug
embeddings:
  enabled: false
  backend: ollama
  model: nomic-embed-text
INNER
      echo "  wrote $yaml_path (embeddings disabled)"
    fi
  done
fi

echo "0.1.0-to-0.2.0 migration applied."
```

Test idempotency manual: corrida 1 escribe, corrida 2 noop.

### Task 7.3: Test E2E integration

**Files:**
- Create: `tests/integration/test_v0_2_end_to_end.py`

Cubre los 15 ítems del acceptance checklist. Sub-tests por ítem usando fixtures (`fake_claude` extendido para wizard, mock Ollama via httpx, mock launchctl/crontab via env var override). Ítems platform-specific (#9) skipean en otro OS.

### Task 7.4: Documentation updates

- `docs/primitives/*.md`: actualizar "Estado vX" de cada primitive
- `docs/cli-reference.md`: agregar 6 commands nuevos
- `docs/getting-started.md`: revisar flujo bootstrap, sacar menciones a "skeleton only"
- `CLAUDE.md` (raíz): cerrar items deferred que ahora están; mantener los que quedan para v0.3+
- `src/rufino/wizard/materializer.py`: actualizar docstring de `materialize()`

### Task 7.5: Fin de fase 7 — Final code review + commit + push + PR

- [ ] `pytest --cov=src --cov-report=term-missing` — coverage ≥80% global, ≥85% módulos nuevos
- [ ] Code-reviewer agent — final review de la release
- [ ] Fixes
- [ ] Commit `release: rufino v0.2.0 — big bang functional release` con cuerpo enumerando los 12 gaps cerrados
- [ ] Push final
- [ ] `gh pr create` con título "feat: rufino v0.2.0 functional release" + body referenciando el design doc

---

## Self-review

**Spec coverage** — los 15 ítems del acceptance checklist están cubiertos:
- 1 (wizard sin corpus): Task 2.4
- 2 (wizard con corpus): regla operativa #10 + smoke manual
- 3 (embedder sí): Task 3.5 + 3.7
- 4 (embedder no): Task 3.6 (semantic con Noop → exit 2)
- 5 (forward traversal): Task 5.1
- 6 (single-note full): Task 4.4
- 7 (emit_augmented): Task 4.3
- 8 (transform_hook): Task 4.1 + 4.2
- 9 (scheduler real): Task 6.2 + 6.4
- 10 (concurrency): Task 1.1 + 1.2
- 11 (memoria acotada): Task 1.3
- 12 (worker padding): Task 1.4
- 13 (migración limpia): Task 7.2
- 14 (regresión cero): `pytest` al final de cada fase
- 15 (E2E test): Task 7.3

**Placeholder scan** — ninguno encontrado.

**Type consistency** — `IngestSpec`, `ProcessSpec`, `OutputSpec` usados consistentemente en todas las fases que los referencian. `NoopEmbedder`/`OllamaEmbedder` consistentes.

**Workflow consistency** — cada fase termina con la misma cadena: `pytest` → code-reviewer agent → fixes → `pytest` → commit → push. Convención `feat(v0.2): <area>` en commits.

---

## Execution Handoff

Plan complete y guardado en `docs/superpowers/plans/2026-05-19-rufino-v0.2-functional.md`. Dos opciones:

**1. Subagent-Driven (recommended)** — un subagent fresco por task, review entre tasks, iteración rápida.

**2. Inline Execution** — tasks en esta sesión vía `executing-plans`, batch con checkpoints.

¿Cuál preferís?
